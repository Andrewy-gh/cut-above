# Convex Setup (Phase 1a)

- Start local Convex dev server: `pnpm dev:convex`.
- Convex CLI reads environment variables like `CONVEX_DEPLOYMENT` and `CONVEX_URL` when connecting.
- This folder is bootstrapped for Phase 1b schema work.
